import 'package:flutter/material.dart';

class bodega extends StatelessWidget {
  const bodega({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bienvenido a bodega'),
      ),
    );
  }
}