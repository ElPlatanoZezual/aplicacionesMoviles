import 'package:flutter/material.dart';

class ventas extends StatelessWidget {
  const ventas ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bienvenido a ventas'),
      ),
    );
  }
}